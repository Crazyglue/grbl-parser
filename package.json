{
  "name": "grbl-parser",
  "version": "0.0.7",
  "description": "Javascript-based gcode parser for grbl",
  "main": "./lib/parser",
  "scripts": {
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha test/* --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "author": "Eric Dobroveanu",
  "license": "ISC",
  "devDependencies": {
    "lodash": "^4.17.4",
    "chai": "^3.5.0",
    "istanbul": "^0.4.5",
    "istanbul-coveralls": "^1.0.3",
    "mocha": "^3.2.0"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/Crazyglue/grbl-parser.git"
  }
}
